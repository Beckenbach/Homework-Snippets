var receiptItems = [2.99, 3.67, 12.56, 4.78, 4.69, 3.70, 26.42];
var produce = [.99,.67,.56,.78,.69,.70];
var taxRate = 0.75;
var subTotal = 0;


function priceItems (list)  { 
	for (i = 0 i < list.length i++);
		document.write [i].toFixed + "/br";
	}
}
priceItems receiptItems
document.write;		
	
	function subTotal (list)  { 
	for (i = 0 i < list.length i++);
		subTotal = subTotal + priceItems;
	}
}
calcSubtotal receiptItems

	function taxRate (list)  { 
		total = subTotal * taxRate;
	}
}

	